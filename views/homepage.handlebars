<h2>Recent Posts:</h2>

{{#each displayPosts as |post|}}
  <section class="card">
    <p class="cardsection">Subject: {{post.subject}}</p>
    <p class="cardsection">Posted By: {{post.author_name}}</p>
    <p class="cardsection">{{post.post}}</p>

  </section>

{{/each}}